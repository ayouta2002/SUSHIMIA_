// Définition d'une méthode qui renvoie une ObservableList contenant des objets productData.
//public ObservableList<productData> inventoryDataList() {
        // Création d'une ObservableList vide pour stocker les objets productData.
        ///ObservableList<productData> listData = FXCollections.observableArrayList();

        // La requête SQL pour sélectionner toutes les données de la table produit.
       /* String sql = "SELECT * FROM product";

        // Tente de se connecter à la base de données et d'exécuter la requête.
        connect = database.connectDB();

        try {
// Préparation de la requête SQL pour l'exécution*/
